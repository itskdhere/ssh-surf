{
  "name": "ssh-surf",
  "version": "1.0.0",
  "description": "ssh surf",
  "private": "true",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prod": "pm2 start server.js --watch -n dashboard && pm2 startup && pm2 save",
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "keywords": [],
  "author": "itskdhere",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^18.0.6",
    "express": "^4.18.2",
    "node-fetch": "^3.2.6"
  }
}
